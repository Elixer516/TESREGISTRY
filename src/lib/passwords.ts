/**
 * What every new or reset trainee account starts with. Known to everyone by
 * design — which is why an account on it can do nothing but replace it.
 */
export const DEFAULT_TRAINEE_PASSWORD = 'user1234';

/** The shortest password a person may choose for themselves. */
export const MIN_PASSWORD_LENGTH = 8;
